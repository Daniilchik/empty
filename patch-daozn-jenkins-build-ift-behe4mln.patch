diff --git a/jenkins/build/functions.groovy b/jenkins/build/functions.groovy
index cce8db6..1307b11 100644
--- a/jenkins/build/functions.groovy
+++ b/jenkins/build/functions.groovy
@@ -18,6 +18,10 @@ def ozoneNamespace(stand) {
     switch (stand) {
         case 'DEV':  return 'dev'
         case 'IFT':  return 'ci06618727-ozone'
+        // TODO: namespace второго кластера IFT (behe4mln). До заполнения
+        // сборка падает намеренно — иначе kubelatte-правила соберутся
+        // под namespace 'default' из ветки по умолчанию.
+        case 'IFT-BEHE4MLN': error('BUILD_FOR_STAND=IFT-BEHE4MLN: укажите namespace в ozoneNamespace()')
         case ['PSI', 'PROD', 'RDT']: return 'ci15207061-ozone'
         default:     return 'default'
     }
diff --git a/jenkins/build/helm-chart.Jenkinsfile b/jenkins/build/helm-chart.Jenkinsfile
index 7532fff..4a96645 100644
--- a/jenkins/build/helm-chart.Jenkinsfile
+++ b/jenkins/build/helm-chart.Jenkinsfile
@@ -3,7 +3,7 @@
 echo "env.JOB_NAME=${env.JOB_NAME}"
 cred_name = 'cab-sa-dvo06579'
 nexus_cred_name = 'cab-sa-dvo11855'
-if ((params.BUILD_FOR_STAND == 'DEV' || params.BUILD_FOR_STAND == 'IFT') && !params.FORCE_CHECKS) {
+if ((params.BUILD_FOR_STAND in ['DEV', 'IFT', 'IFT-BEHE4MLN']) && !params.FORCE_CHECKS) {
     nodeLabel = 'Linux_Default'
 } else {
     nodeLabel = 'clearAgent'
diff --git a/jenkins/build/params.groovy b/jenkins/build/params.groovy
index efa1dc4..72c7209 100644
--- a/jenkins/build/params.groovy
+++ b/jenkins/build/params.groovy
@@ -1,7 +1,7 @@
 jobParameters = ([
     parameters([
         choice(
-            choices: ['DEV', 'IFT', 'PSI', 'PROD', 'RDT'],
+            choices: ['DEV', 'IFT', 'IFT-BEHE4MLN', 'PSI', 'PROD', 'RDT'],
             name: 'BUILD_FOR_STAND'
         ),
         string(
