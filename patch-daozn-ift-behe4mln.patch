diff --git a/values/values-ift-behe4mln.yaml b/values/values-ift-behe4mln.yaml
new file mode 100644
index 0000000..742a94a
--- /dev/null
+++ b/values/values-ift-behe4mln.yaml
@@ -0,0 +1,257 @@
+# Второй кластер IFT (behe4mln). Контур тот же — qa.df.sbrf.ru,
+# поэтому realm, KDC, LDAP, Ranger и secman совпадают с values-ift.yaml.
+# Хранилище — emptyDir: PVC и привязка к узлам не используются.
+# Метки TODO(ift2) отмечают то, что нужно уточнить.
+
+global:
+  image:
+    registry: docker-release.registry-ci.delta.sbrf.ru/ci15207061/ci15207061
+    repository: ozone@sha256
+    tag: 9620ee3744bc3e92122d72ffa23774636b8a5a35ebffd00948ecd3c688f33440
+    pullSecrets:
+      - name: registry-secret
+  ingress:
+    baseDomain: "apps.behe4mln.k8s.delta.sbrf.ru"
+  clusterName: "behe4mln"
+  storage:
+    mode: emptyDir
+  security:
+    kerberos:
+      realm: "QA.DF.SBRF.RU"
+      principal:
+        pattern: "{{ .Service }}/{{ .Subdomain }}.qa.df.sbrf.ru@{{ .Realm }}"
+      kdc:
+        hostname: "tvlds-acc000035.qa.df.sbrf.ru"
+        ipAddress: 10.86.3.115
+
+metalLb:
+  enabled: true
+  image:
+    repository: kube-api@sha256
+    tag: 673eaf462baa094055832020ad8920ecbf0f6a9c37dff70c89125549ffcd7ddb # 1.4.1.13.1
+
+secman:
+  hostname: t.secrets.delta.sbrf.ru
+  path: A/CI07978178/OSH/SDPCOMPUTE/KV/ozone
+  certPath: CI04159629_CI07978178/A/CI07978178/OSH/SDPCOMPUTE/SBERCA/sberca-test-ext-g2/fetch/server-client_1y_DROPAPP_rsa
+  namespace: CI04159629_CI07978178
+
+ldap:
+  hosts:
+    - tvlds-acc000035.qa.df.sbrf.ru
+  port: 636
+  baseDn: "cn=accounts,dc=qa,dc=df,dc=sbrf,dc=ru"
+  useStartTls: false
+  users:
+    filter: "(objectClass=person)"
+    nameAttr: "uid"
+  groups:
+    filter: "(objectClass=posixgroup)"
+    nameAttr: "cn"
+    memberAttr: "member"
+
+istio:
+  egressGateway:
+    tls:
+      secman:
+        rangerCaKey: "ca_chain"
+        fluentbitCaKey: "ca_chain"
+        prometheusRwCaKey: "ca_chain"
+        ipaCaKey: "ldap_ca_chain"
+
+om:
+  replicas: 3
+  resources:
+    requests:
+      memory: "4Gi"
+      cpu: "1"
+    limits:
+      memory: "4Gi"
+      cpu: "2"
+
+scm:
+  replicas: 3
+  resources:
+    requests:
+      memory: "4Gi"
+      cpu: "1"
+    limits:
+      memory: "4Gi"
+      cpu: "2"
+  primordialNode: scm0
+
+datanode:
+  replicas: 4
+  resources:
+    requests:
+      memory: "6Gi"
+      cpu: "2"
+    limits:
+      memory: "12Gi"
+      cpu: "4"
+
+recon:
+  enabled: false
+  replicas: 1
+  resources:
+    requests:
+      memory: "4Gi"
+      cpu: "1"
+    limits:
+      memory: "4Gi"
+      cpu: "2"
+  ingress:
+    enabled: true
+    secman:
+      tls: true
+
+s3g:
+  replicas: 1
+  resources:
+    requests:
+      memory: "4Gi"
+      cpu: "1"
+    limits:
+      memory: "4Gi"
+      cpu: "2"
+  hpa:
+    enabled: true
+    minReplicas: 1
+    maxReplicas: 10
+  ingress:
+    enabled: true
+    secman:
+      tls: true
+
+hue:
+  enabled: false
+  image:
+    repository: hue@sha256
+    tag: a84d1af164447f29fafbacb3c73f518ea075458f0ec85c1cdc784ad129bc9bd3
+  resources:
+    requests:
+      memory: "2Gi"
+      cpu: "500m"
+    limits:
+      memory: "2Gi"
+      cpu: "1"
+  ingress:
+    enabled: true
+    secman:
+      tls: true
+  database:
+    # TODO(ift2): БД общая с существующим ift. Для HMS кластеры разведены
+    # схемой (ниже), а Hue делит базу целиком — search_path в шаблоне
+    # захардкожен. При включении Hue нужна отдельная база или правка чарта.
+    hosts:
+     - "10.26.93.188"
+     - "10.26.93.190"
+    port: 5433
+    name: hue
+    user: ci07978178_pg_ozone_z
+    password: ''
+  secman:
+    dbPasswordKey: hue_db_password
+    sessionSecretKey: hue_session_secret_key
+  ldap:
+    enabled: false
+
+hiveMetastore:
+  enabled: true
+  replicas: 1
+  component: hivemetastore
+  serviceName: hivemetastore
+  image:
+    repository: hive-metastore@sha256
+    tag: bdcf4e30ab2b97f3fb2787f7b2688a40d368189d1d7f61ad42bf1879b57d501d
+  resources:
+    requests:
+      memory: "2Gi"
+      cpu: "500m"
+    limits:
+      memory: "2Gi"
+      cpu: "1"
+  service:
+    thriftPort: 9083
+  database:
+    hosts:
+     - "10.26.93.188"
+     - "10.26.93.190"
+    port: 5433
+    name: hive
+    user: ci07978178_pg_ozone_z
+    password: ''
+    # Отдельная схема на кластер — иначе два Ozone делят один метастор
+    # и видят таблицы друг друга, указывающие на чужие ofs://-волюмы.
+    schema: "{{ include \"ozone.namespace\" . | replace \"-\" \"_\" }}_{{ .Values.global.clusterName }}_hive"
+  secman:
+    path: A/CI07978178/OSH/SDPCOMPUTE/KV/ozone
+    dbPasswordKey: hive_db_password
+
+ranger:
+  enabled: true
+  # In-mesh URL: HTTP, port 80. Egress Gateway делает mTLS origination
+  # к настоящему https://ranger... (клиентский cert+key+CA из выдачи
+  # SBERCA, см. istio.egressGateway.tls.secman.rangerMtls).
+  policyAdminUrl: "http://ranger.qa.df.sbrf.ru"
+  ozone:
+    # TODO(ift2): подтвердить. Сейчас то же имя, что у существующего ift —
+    # значит политики двух кластеров в Ranger общие. Если нужна изоляция,
+    # завести отдельный сервис и указать его здесь.
+    serviceName: "iftdrpoznb2c_ozone"
+  ipa:
+    enabled: true
+    url: "ldap://tvlds-acc000035.qa.df.sbrf.ru:636"
+    baseDn: "dc=qa,dc=df,dc=sbrf,dc=ru"
+    secman:
+      bindDnKey: hue_ldap_bind_dn
+      bindPasswordKey: hue_ldap_bind_password
+
+cli:
+  image:
+    repository: ozone-cli@sha256
+    tag: 33253c63c3c2105b25e5ddf90b7de5c137332e424ec8bbe7c3ad2faccb7fddae
+  secman:
+    path: A/CI07978178/OSH/SDPCOMPUTE/KV/ozone
+    keytabName: ozone_cli_admin_keytab_b64
+
+prometheus:
+  enabled: true
+  image:
+    repository: prometheus@sha256
+    tag: "80aa7801c1781077911aca62aef889cdb621ada6e7fa359999d648189d23e64a"
+  ingress:
+    enabled: false
+  resources:
+    requests:
+      cpu: 100m
+      memory: 100Mi
+      ephemeral-storage: 500Mi
+    limits:
+      cpu: 200m
+      memory: 180Mi
+      ephemeral-storage: 500Mi
+  storage:
+    sizeLimit: 4500M
+  # OrgID — must match X-Scope-OrgID header in remoteWrite
+  remoteWriteId: ozonedelta
+  remoteWrite:
+    # In-mesh URL: HTTP, egress gateway делает mTLS origination
+    # к настоящему https://rw-selfservice... (клиентский cert+key+CA из
+    # выдачи SBERCA, см. istio.egressGateway.tls.secman.prometheusRwMtls).
+    - url: http://rw-selfservice.delta.sbrf.ru/api/v1/push
+      remote_timeout: 30s
+      headers:
+        X-Scope-OrgID: ozonedelta
+  federate:
+    enabled: true
+    url: "http://metrics.behe4mln.k8s.delta.sbrf.ru:8481"
+
+fluentBit:
+  enabled: true
+  image:
+    repository: fluent-bit@sha256
+    tag: 9d2d9adbce561b69e534ee91326e6b526c312f92f612ff6fd0fdd9aa599d83f3
+  # TODO(ift2): AppID нового кластера — выдаёт команда restlogs.
+  appId: "SPD_OZONE-CI06618727-CI06618727-202410160000000000"
+  restlogsHost: tvlss-log000003.dev.df.sbrf.ru
